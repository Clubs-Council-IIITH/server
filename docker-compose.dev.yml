version: '3'

services:
  db:
    image: postgres:13.3-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    ports:
      - 5432:5432
    env_file:
      - ./.env

  backend:
    build: ./backend
    command: >
      sh -c "cd live_backend &&
             python manage.py makemigrations &&
             python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    volumes:
      - ./backend:/backend/live_backend
    ports:
      - 8000:8000
    env_file:
      - ./.env
    depends_on:
      - db

  frontend:
    build: ./frontend
    command: >
      sh -c "cd live_frontend && 
             yarn && 
             yarn start"
    network_mode: host
    volumes:
      - ./frontend:/frontend/live_frontend
    ports:
      - 3000:3000
    env_file:
      - ./.env

volumes:
  postgres_data:
  media_volume:
  static_volume:
